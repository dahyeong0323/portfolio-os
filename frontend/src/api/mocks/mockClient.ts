import type { MockKey } from "../client";
import {
  mockAccounts,
  mockExecutionDetail,
  mockExecutions,
  mockHealth,
  mockInstruments,
  mockLedgerSnapshot,
  mockLedgerStatus,
  mockJournal,
  mockJournalEntry,
  mockGovernanceEvents,
  mockGovernanceOverview,
  mockMacroDetail,
  mockMacroItems,
  mockReconciliation,
  mockReconciliationReport,
  mockOverrideDetail,
  mockOverrides,
  mockPostmortems,
  mockResearchDetail,
  mockResearchItems,
  mockRiskValidation,
  mockReportCategories,
  mockReportDetail,
  mockReports,
  mockSeniorMemoDetail,
  mockSeniorMemos,
  mockTicketDetail,
  mockTickets,
} from "./mockData";

const mockResponses = {
  health: mockHealth,
  ledgerStatus: mockLedgerStatus,
  ledgerSnapshot: mockLedgerSnapshot,
  accounts: mockAccounts,
  instruments: mockInstruments,
  reconciliation: mockReconciliation,
  reconciliationById: mockReconciliation.reconciliation,
  reconciliationReport: mockReconciliationReport,
  reportCategories: mockReportCategories,
  reports: mockReports,
  reportDetail: mockReportDetail,
  researchItems: mockResearchItems,
  researchDetail: mockResearchDetail,
  macroItems: mockMacroItems,
  macroDetail: mockMacroDetail,
  seniorMemos: mockSeniorMemos,
  seniorMemoDetail: mockSeniorMemoDetail,
  governanceOverview: mockGovernanceOverview,
  governanceEvents: mockGovernanceEvents,
  tickets: mockTickets,
  ticketDetail: mockTicketDetail,
  riskValidation: mockRiskValidation,
  executions: mockExecutions,
  executionDetail: mockExecutionDetail,
  overrides: mockOverrides,
  overrideDetail: mockOverrideDetail,
  journal: mockJournal,
  journalDetail: mockJournalEntry,
  postmortems: mockPostmortems,
} satisfies Record<MockKey, unknown>;

export async function getMockResponse<T>(key: MockKey): Promise<T> {
  await new Promise((resolve) => window.setTimeout(resolve, 120));
  return structuredClone(mockResponses[key]) as T;
}
